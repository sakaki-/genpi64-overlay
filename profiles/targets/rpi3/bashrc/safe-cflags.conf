# cite "safe-cflags.conf" for package <foo> in
# /etc/portage/package.env/<foo>, to force lower optimization level
# (useful for some packages that force -O3 by default)

CFLAGS="${CFLAGS} -O2"
CXXFLAGS="${CFLAGS}"
